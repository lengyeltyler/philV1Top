# Testing (Top Trait)

## Prereqs
- Node 20 (repo uses .nvmrc)
- `.env` contains:
  - SEPOLIA_RPC_URL=
  - PRIVATE_KEY=
  - ETHERSCAN_API_KEY=   (optional, only needed for contract verification)

Install deps:
```bash
npm install